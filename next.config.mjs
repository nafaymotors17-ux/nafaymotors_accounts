/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  reactStrictMode: false, // Disable React Strict Mode to prevent double renders in development
};

export default nextConfig;
